version: "3.9"

services:
  age-estimator:
    build: .
    container_name: age_estimator
    ports:
      - "8501:8501"
    volumes:
      - ./checkpoints:/app/checkpoints
    restart: unless-stopped
